enum EnumCookies {
	TOKEN = "token",
	THEME = "theme",
	ROLE = "role"
}

enum EnumRole {
	PRINTER = 1,
	DESIGNER = 2,
	DIRECTOR = 3
}

enum EnumTheme {
	DARK = "dark",
	LIGHT = "light"
}

export { EnumCookies, EnumRole, EnumTheme }
